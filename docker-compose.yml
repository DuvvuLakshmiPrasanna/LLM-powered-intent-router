version: "3.8"

services:
  intent-router:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    volumes:
      - ./route_log.jsonl:/app/route_log.jsonl
    restart: unless-stopped
